name: Checkout Only

on:
  push:
    branches:
      - main  # or your desired branch
  pull_request:

jobs:
  checkout:
    runs-on: mac-docker-runner

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4